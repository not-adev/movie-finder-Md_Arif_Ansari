# AI_LOG

## Tools Used
- VS Code editor and file inspection tools
- Next.js project setup and configuration review
- React component editing and debugging
- Terminal build verification

## Best Prompts
- "Build a movie discovery app in Next.js with search, pagination, details modal, and favorites using TMDB API."
- "Make the UI responsive, add loading/error/empty states, and include a footer with the exact required text."
- "Fix the code so it uses manual pagination with exactly 12 results per page and persists favorites in localStorage."

## What I Fixed Manually
- Corrected the page logic so the app uses manual pagination instead of automatic loading.
- Updated the footer text to exactly match the required format.
- Adjusted the modal behavior so details loading and display are handled cleanly.
